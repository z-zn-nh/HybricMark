import { I18nText } from '../../../components/site/I18nProvider'

# <I18nText en="Custom Slash Menu" zh="自定义斜杠菜单" />

<I18nText en="HybricMark is context-first by default, but slash interactions can be extended through Tiptap extensions." zh="HybricMark 默认是上下文优先交互，斜杠菜单能力可通过 Tiptap 扩展继续增强。" />

## <I18nText en="Integration path" zh="集成路径" />

- <I18nText en="Implement a custom extension/plugin for slash command detection." zh="实现自定义扩展/插件用于斜杠命令识别。" />
- <I18nText en="Pass it through `extensions`." zh="通过 `extensions` 属性注入。" />
- <I18nText en="Keep command rendering state in your app layer when menu complexity grows." zh="当菜单复杂度上升时，将命令渲染状态上提到业务层管理。" />

## <I18nText en="Example shape" zh="结构示例" />

```tsx
import type { Extension } from '@tiptap/core'

const MySlashExtension: Extension = /* your extension */

<HybricEditor
  content={doc}
  extensions={[MySlashExtension]}
/>
```

## <I18nText en="Recipe: add `Insert YouTube` command" zh="实战：新增“插入 YouTube”命令" />

```tsx
import type { Editor } from '@tiptap/core'

function insertYouTube(editor: Editor, url: string) {
  const embed = `https://www.youtube.com/embed/${extractVideoId(url)}`
  editor
    .chain()
    .focus()
    .insertContent({
      type: 'paragraph',
      content: [{ type: 'text', text: `YouTube: ${embed}` }],
    })
    .run()
}

function insertWarningBox(editor: Editor) {
  editor
    .chain()
    .focus()
    .insertContent({
      type: 'blockquote',
      content: [
        {
          type: 'paragraph',
          content: [{ type: 'text', text: 'Warning: verify this step before publishing.' }],
        },
      ],
    })
    .run()
}
```

<I18nText en="In practice, bind these commands into your slash command menu source and dispatch them from command handlers." zh="实践中，把这些命令注册到你的斜杠菜单数据源，在命令回调中触发即可。" />

## <I18nText en="Product-level recommendations" zh="产品级建议" />

- <I18nText en="Group commands by intent (`format`, `insert`, `actions`)." zh="按意图分组命令（`format`、`insert`、`actions`）。" />
- <I18nText en="Reuse labels and shortcuts from context menu for consistency." zh="复用右键菜单标签与快捷键，保证一致性。" />
- <I18nText en="Keep top commands keyboard-first for speed." zh="高频命令优先键盘触发以提升效率。" />
