# 2026-02-13 注脚与列表修复回合3

## 修复内容
- 注脚回跳按钮改为定义行右上角绝对定位，避免挤占描述输入位置。
- 注脚定义编辑中禁用回跳行为（`data-hm-footnote-back-editing=true`）。
- 列表 `Shift+Tab` 顶级项增加强制退出为普通段落逻辑。
- 清除整段样式改为：优先脱离全部列表层级，最终清为普通段落（当前段生效）。

## 自动化验证
- 注脚定义行可输入空格（`[^a]: 11 22`）且按钮在右上角。
- 清除三级列表后，当前项变普通段落，其他列表结构保留。
- 顶级列表 `Shift+Tab` 后变左对齐普通段落。
- 控制台无 error。

## 校验
- npm run lint 通过
- npm run build 通过
